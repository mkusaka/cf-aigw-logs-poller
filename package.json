{
	"name": "cf-aigw-logs-poller",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:integration": "RUN_INTEGRATION_TESTS=true vitest run test/integration.spec.ts",
		"test:unit": "vitest run test/index.spec.ts",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.29.1"
	}
}